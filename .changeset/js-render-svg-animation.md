---
"takumi-js": minor
---

Add top-level `renderSvg` and `renderAnimation` functions, mirroring `render`: same JSX/HTML/node input and resource pipeline, returning a vector SVG string and an encoded animation respectively.
