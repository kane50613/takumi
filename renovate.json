{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": ["config:recommended", "group:allNonMajor"],
  "labels": ["dependencies"],
  "minimumReleaseAge": "7 days",
  "description": "minimumReleaseAge does not cover lockfile updates. Cargo resolves through update-lockfile, so a compatible crate can enter Cargo.lock before the window is up.",
  "packageRules": [
    {
      "matchManagers": ["cargo"],
      "matchPackageNames": ["phf", "strict-num", "float-cmp"],
      "matchUpdateTypes": ["major", "minor"],
      "enabled": false,
      "description": "Held at the versions cssparser, selectors and tiny-skia-path resolve to. A newer major lands a second copy of the crate in the tree."
    },
    {
      "matchManagers": ["cargo"],
      "matchPackageNames": [
        "taffy",
        "parley",
        "fontique",
        "skrifa",
        "read-fonts",
        "font-types",
        "write-fonts"
      ],
      "enabled": false,
      "description": "The layout and font stack moves by hand: taffy is pinned by decision, and the fontations family is chained to the hinting-gate fork and parley's semver."
    }
  ]
}
