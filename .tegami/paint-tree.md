---
packages:
  "takumi": minor
---

# Read painted values from a node tree

`takumi_core::paint_tree::paint_tree`, behind the `paint-tree` feature, returns each box's used decorations, image placement, and shaped text runs in paint order, in device pixels with absolute transforms. `Fonts::face_family` names the registered family of a shaped run's face.
