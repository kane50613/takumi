---
packages:
  takumi-pdf:
    type: minor
---

### Render fillable checkboxes and radio groups

With `form: true`, checkbox and radio inputs become editable PDF buttons. `value` sets the export value, and `checked` sets the initial selection. Radio buttons with the same name form one group.
