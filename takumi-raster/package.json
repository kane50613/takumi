{
  "name": "takumi-raster",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "version": "cargo set-version -p takumi-raster $(jq -r '.version' package.json)",
    "release": "cargo publish || [ $? -eq 101 ] || exit $?"
  },
  "dependencies": {
    "takumi-core": "workspace:*"
  }
}
