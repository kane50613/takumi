{
  "name": "example-cloudflare-workers",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "start": "wrangler dev",
    "cf-typegen": "wrangler types",
    "build": "wrangler build",
    "test": "vitest run"
  },
  "dependencies": {
    "react": "catalog:",
    "takumi-js": "workspace:*",
    "takumi-template": "workspace:*"
  },
  "devDependencies": {
    "@types/react": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:",
    "wrangler": "^4.83.0"
  }
}
