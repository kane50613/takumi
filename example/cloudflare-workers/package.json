{
  "name": "example-cloudflare-workers",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "portless run --name workers.takumi sh -c 'wrangler dev --port $PORT'",
    "start": "wrangler dev",
    "cf-typegen": "wrangler types",
    "build": "wrangler build",
    "test": "vitest run"
  },
  "dependencies": {
    "react": "catalog:",
    "takumi-js": "workspace:*",
    "takumi-template": "workspace:*"
  },
  "devDependencies": {
    "@types/react": "catalog:",
    "portless": "^0.15.4",
    "typescript": "catalog:",
    "vitest": "^4.1.10",
    "wrangler": "^4.114.0"
  }
}
