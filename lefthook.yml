pre-commit:
  parallel: true
  jobs:
    - run: bun run lint:fix
      stage_fixed: true

    - run: cargo fmt --all
      stage_fixed: true

    - root: takumi/
      run: cargo rdme --force
      stage_fixed: true
