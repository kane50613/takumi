pre-commit:
  piped: true
  jobs:
    - run: cargo fmt --all
      stage_fixed: true

    - root: takumi/
      run: cargo rdme --force && git add README.md

    - run: bun run lint:fix
      stage_fixed: true
